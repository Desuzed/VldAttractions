<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vldattractions">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_vld"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_vld"
        android:supportsRtl="true"
        android:theme="@style/Theme.VldAttractions">
        <activity android:name=".ActivityAbout"></activity>
        <activity
            android:name=".LogoActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.VldAttractions.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListContentActivity"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.VldAttractions.NoActionBar"
            android:screenOrientation="portrait"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>